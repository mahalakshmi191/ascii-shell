push :
  branches: ["main"]
jobs: 
  ascii_job:
  run-on: ubuntu-latest
  steps: 
  - name: checkout repo
    use: actions/checkout@v4
  - name: executing shell
    run: |
         chmod +x ascii.script.sh
         ./ascii.script.sh
